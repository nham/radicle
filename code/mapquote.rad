((label map (lambda (f xs) (cond ((atom xs) xs)
                                  ('t (cons (f (car xs)) 
                                 (map f (cdr xs)))))))
  (quote (lambda (x) (cons x (quote (y z)))))
  (quote (a b c)))
