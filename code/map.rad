((label map (lambda (f xs) (cond ((atom xs) xs)
                                  ('t (cons (f (first xs)) 
                                 (map f (rest xs)))))))
  '(lambda (x) (cons x '(y z)))
  '(a b c)
)
