((label subst (lambda (x y z)
                (cond ((atom z)
                       (cond ((eq z y) x)
                             ('t z)))
                      ('t (cons (subst x y (first z))
                                       (subst x y (rest z)))))))
 'm 'b '(a b (a b c) d))
